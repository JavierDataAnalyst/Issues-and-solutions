-- hay casos en los que quieres generar valores “a mano” sin depender de ninguna tabla. 
-- UNION ALL lo que hace es juntar los resultados de varias consultas uno debajo del otro (concatenación de filas).
-- Esta estructura sirve para organizarlo por número (días, meses, años, productos, empleados, ID, etc)
SELECT
		 1 AS mes_num,
		 'January' AS mes_name
UNION ALL
 SELECT
		 2,
		 'February'
UNION ALL
 SELECT
		 3,
		 'March'
UNION ALL
 SELECT
		 4,
		 'April'
UNION ALL
 SELECT
		 5,
		 'May'
UNION ALL
 SELECT
		 6,
		 'June'
UNION ALL
 SELECT
		 7,
		 'July'
UNION ALL
 SELECT
		 8,
		 'August'
UNION ALL
 SELECT
		 9,
		 'September'
UNION ALL
 SELECT
		 10,
		 'October'
UNION ALL
 SELECT
		 11,
		 'November'
UNION ALL
 SELECT
		 12,
		 'December'
